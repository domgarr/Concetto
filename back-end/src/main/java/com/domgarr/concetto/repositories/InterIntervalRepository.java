package com.domgarr.concetto.repositories;

import com.domgarr.concetto.models.InterInterval;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InterIntervalRepository extends JpaRepository<InterInterval, Long> {

}
